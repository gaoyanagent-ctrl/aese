import{expect,test}from"@playwright/test";test("assurance closes renewed and injected paths",async({page})=>{await page.goto("/#world-assurance");await expect(page.getByRole("heading",{name:"Strategy Assurance Observatory"})).toBeVisible();await expect(page.getByText("renewed",{exact:true}).first()).toBeVisible();await expect(page.getByText("12 / 0")).toBeVisible();await expect(page.getByText("60 runs / 0 failed")).toBeVisible();await page.getByRole("button",{name:"切换保障路径"}).click();await expect(page.getByText("reexperiment_required",{exact:true}).first()).toBeVisible();await page.getByRole("button",{name:"切换保障路径"}).click();await expect(page.getByText("retired",{exact:true}).first()).toBeVisible()});
