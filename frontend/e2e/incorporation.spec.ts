import {expect,test} from '@playwright/test';
test('Genesis incorporation reaches machine-verifiable M10 eligibility',async({page})=>{const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/#world');await page.getByRole('button',{name:'企业成立 Campaign'}).click();await expect(page).toHaveURL(/#world-incorporation$/);await expect(page.getByRole('heading',{name:'投资人形成设立意图'})).toBeVisible();await expect(page.getByText('预算不是现金或实际支出')).toBeVisible();for(let i=0;i<7;i++)await page.getByRole('button',{name:/单步/}).click();await expect(page.getByText('M10 eligible')).toBeVisible();await expect(page.getByText('是',{exact:true})).toBeVisible();expect(errors).toEqual([])});
