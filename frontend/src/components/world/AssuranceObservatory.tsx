import{ArrowLeft,BadgeCheck,Database,ShieldCheck}from"lucide-react";import{useEffect,useState}from"react";import{loadAssurance,type AssuranceCycle}from"../../world/assurance";import"./WorldPlay.css";
export function AssuranceObservatory({onExit}:{onExit:()=>void}){const[c,setC]=useState<AssuranceCycle|null>(null),[path,setPath]=useState(0);useEffect(()=>{const x=new AbortController();loadAssurance(x.signal).then(setC).catch(e=>{if(e.name!=="AbortError")throw e});return()=>x.abort()},[]);if(!c)return <main className="world-loading">正在封存 AssuranceDataset…</main>;const decision=path===0?c.decision:c.injected_decisions[path-1];return <div className="world-play"><header className="world-toolbar"><button className="world-back" onClick={onExit}><ArrowLeft/>Strategy Control</button><div><span>CONTINUOUS ASSURANCE · NO AUTO LEARNING</span><h1>Strategy Assurance Observatory</h1></div><div className="world-clock"><small>Disposition</small><strong>{decision.disposition}</strong></div></header><nav className="world-controls"><button onClick={()=>setPath((path+1)%3)}>切换保障路径</button><span>12 weeks · calibration 8 / holdout 4</span></nav><main className="world-main"><section className="world-status"><span className="world-state-badge closed"><BadgeCheck/>strategy_assurance_cycle_closed = true</span><h2>{c.code}</h2><p>{c.release} · cutoff {c.cutoff_at} · cursor {c.cursor_range}</p></section><section className="three-state-grid"><article><header><Database/>Dataset & lineage</header><dl><div><dt>Observations / missing</dt><dd>{c.observations.length} / {c.missingness}</dd></div><div><dt>Dataset hash</dt><dd>{c.dataset_hash.slice(0,16)}…</dd></div><div><dt>Correction sets</dt><dd>0 · sealed as-of cutoff</dd></div></dl></article><article><header><ShieldCheck/>Drift assessments</header><dl>{c.drift.map(d=><div key={d.domain}><dt>{d.domain} · n={d.sample_size}</dt><dd>{d.support} · {d.current}</dd></div>)}</dl></article><article><header>Calibration & holdout</header><dl><div><dt>Fit / holdout</dt><dd>{c.calibration.fit_weeks} / {c.validation.holdout_weeks}</dd></div><div><dt>Holdout locked</dt><dd>{String(c.calibration.holdout_locked)}</dd></div><div><dt>Replay</dt><dd>{c.validation.replay_runs} runs / {c.validation.failed_runs} failed</dd></div></dl></article></section><section className="world-status"><h2>{decision.disposition}</h2><p>{decision.reason}</p><p>{decision.release_effect}</p><p>{c.limitations}</p></section></main></div>}
